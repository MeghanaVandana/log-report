artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."

[metadata]
category = "data_processing"
subcategory = "log_analysis"
task_objective = ["parse_logs"]
artifact_type = ["json"]
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse a small access log and emit summary statistics."
solution_explanation = "Read the access log, count requests and unique IPs, determine the top path, and write the JSON report."
verification_explanation = "Verify that the generated JSON report contains the expected summary values."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []
